import { LoginForm } from './login-form';

const LoginPage = () => (
	<div className="relative mx-auto flex w-full max-w-[400px] flex-col items-center justify-center space-y-2.5 p-4 md:-mt-32 md:h-screen">
		<LoginForm />
	</div>
);

export default LoginPage;
